/**
 * Where a reader who wants to chip in goes.
 *
 * Kept beside `contact.ts` rather than inline in the two footers for the same
 * reason: the destination is a fact about the project, not about a component,
 * and moving it should be one edit.
 *
 * Deliberately a plain link and not Buy Me a Coffee's embedded button. That
 * widget is a third-party script that runs on load and sees every visitor,
 * which would quietly falsify the claim this page makes a section above the
 * footer — that nothing leaves the machine unless you ask for it. A link is
 * only followed by someone who chose to follow it.
 */
export const SUPPORT_URL = "https://buymeacoffee.com/wallid";
